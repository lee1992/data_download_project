✅【多周期蜡烛图・稳定输出・决策导向版 Prompt（完整版）】
0. 任务目标（只输出决策必要信息）
你将基于我输入的 5 分钟基础 K 线数据，先完成标准化聚合处理，生成周度、日度、30 分钟 3 个目标频率的 K 线序列，再用统一的思考流程遍历每一个目标周期，输出以下内容：
各周期当前结构位置（道氏趋势 + 江恩网格位置 + 波浪层级位置）
各周期下一关键时间窗口与关键价格触发 / 失效条件
各周期的结构切换情景树（“下一节点发生后” 的策略变化）
最后输出固定 6 段的全周期综合总结（我已在末尾定义模板）
强制：允许你进行完整推理，但输出中不得展开推导过程，只允许输出 “锁定数据集 + 决策字段”。
1) 最高优先级硬约束（零容忍）
数据来源红线：100% 仅可使用我输入的 5 分钟基础数据及基于该数据聚合生成的 3 个目标周期 K 线数据中的价格、时间、成交量、指标数值；严禁调用外部行情、历史数据、联网信息、或你记忆中的任何标的知识。
100% 可复现：对相同数据集 + 相同 Prompt，输出必须逐字结构一致（字段名 / 顺序 / 表格结构 / 阈值 / 小数位规则一致）。禁止随机措辞、禁止同义改写、禁止新增字段 / 删字段。
输出控制：除本 Prompt 指定的固定模块外，不得输出任何额外段落。不得输出 “分析过程”“为什么” 等解释。
3) 三理论职责拆分（强制执行）
3.1 道氏理论 = 方向约束（Direction Constraint）
只输出：主要趋势 / 次要趋势 / 短期趋势（上涨 / 下跌 / 横盘震荡）+ 一个 “趋势确认结论”（不展开推导）。
有效突破 / 有效跌破（全周期统一阈值，不得更改）：
有效突破：收盘价超过关键价位 ≥ 0.8% 且连续 2 根该周期 K 线维持其上
有效跌破：收盘价低于关键价位 ≥ 0.8% 且连续 2 根该周期 K 线维持其下
3.2 江恩理论 = 坐标网格（Gann Grid）
分割基准：必须使用该周期锁定数据集中的绝对最高价 H与绝对最低价 L
固定档位：0%、25%、38.2%、50%、61.8%、75%、100%
时间周期：仅用 “前 5 关键拐点的时间间隔”，取最频繁间隔 = 核心循环周期
输出未来 3 个变盘时间窗口：T1/T2/T3（以该周期可见时间单位表达）
共振强度评分（0-100，确定性输出，必须给构成三项分数）：
价格接近度：当前价距离最近江恩档位的百分比距离 d%
d≤0.5%：40 分；0.5%-1%：30 分；1%-2%：20 分；2%-3%：10 分；>3%：0 分
时间接近度：当前时间距离最近变盘窗口中心（以该周期 K 线根数计）t
t=0：40 分；t=1：30 分；t=2：20 分；t=3：10 分；t≥4：0 分
量能配合：只允许用图中 “最新成交量在可见范围内的相对位置”
上四分位：20 分；中位：10 分；下四分位：0 分；不可判断：0 分并标记[确信度降低]
共振强度 = 三项相加（0-100）
3.3 波浪理论 = 结构枚举（Wave Enumeration）+ CSP 约束满足
你必须将波浪判定当作 “约束满足问题（CSP）”，并为每条路径输出一个 CSP 分数（0-100）。
CSP 硬约束（全周期统一，不得更改）：
C1：浪型节点必须映射到锁定数据集的前 5 关键拐点（时间顺序一致）
C2：推进浪方向必须与道氏 “主要趋势” 一致（否则该路径扣 40 分）
C3：当前价必须落在该路径定义的 “当前浪区间” 内（否则扣 40 分）
C4：若触发有效突破 / 跌破导致路径切换，必须明确切换后的路径 ID 与新关键位
CSP 评分规则：
初始 100 分；违反 C2 扣 40；违反 C3 扣 40；违反 C1 或 C4 任一扣 20；最低 0 分
4) 强稳定性规则（防止输出漂移）
固定数值保留：完全跟随输入数据的原生精度
输入数据为两位小数→输出两位；为整数→输出整数；不可计算→用不可计算[确信度降低]
路径表达方式（为稳定性优化）：
不输出概率数值（避免模型在临界处波动）
改为固定输出：最可能路径Top1=、最可能路径Top2=、低概率路径=
三路径仍必须完整输出 “触发 / 目标 / 失效”，但不需要概率
禁止自由措辞：以下字段必须按字面模板输出，不得改写。
5) 单周期输出模板（对聚合生成的 3 个目标周期必须严格照搬；不得增加字段）
对聚合生成的 3 个独立目标周期都要完整输出；跨周期关联信息仅在最后的 “共振 / 背离清单” 里引用，不单独成段。
周期级别：{周期名}{如需则加 [确信度降低]}
1) 当前状态・决策属性
道氏方向约束：主要趋势 =；次要趋势 =；短期趋势 =
关键结构位置（波浪层级≥2 层嵌套）：【本周期超级循环浪】→【循环浪X】→【子浪X】→【细分浪X】→【微浪X】（最低层级必须明确）
江恩坐标网格（H/L 分割 7 档位）：0%=；25%=；38.2%=；50%=；61.8%=；75%=；100%=
时间循环与未来 3 窗口：核心循环 =；T1=；T2=；T3=
共振强度评分：总分 =（价格分 =，时间分 =，量能分 =）
CSP 约束满足分数：路径 1=；路径 2=；路径 3=
三路径（固定顺序；字段固定）：
路径 1：触发条件 =；目标区间 =；结构切换临界 =；失效条件 =
路径 2：触发条件 =；目标区间 =；结构切换临界 =；失效条件 =
路径 3：触发条件 =；目标区间 =；结构切换临界 =；失效条件 =
最可能路径：Top1 = 路径 X；Top2 = 路径 Y；低概率路径 = 路径 Z
当前动作建议（固定 5 选 1）：强烈做多 / 偏多 / 中性 / 偏空 / 强烈做空
风控参数（仅引用江恩档位或 P1..P5 价格）：入场参考 =；止损 =；止盈 =
2) 下一节点发生后・决策属性（只允许两条，固定顺序）
情景 A（有效突破）：若有效突破 {关键价位} → 切换到 {路径 ID}；策略变更 =；新止损 =；新目标 =；失效 =
情景 B（有效跌破）：若有效跌破 {关键价位} → 切换到 {路径 ID}；策略变更 =；新止损 =；新目标 =；失效 =
6) 最终综合总结输出（必须严格按以下 6 段，禁止增减）
全周期趋势定位（按大到小，每周期一句）：{周期}：{结构位置}；Top1={路径}；Top2={路径}
多周期共振 / 背离清单：共振={...}；背离={...}；背离风险={触发条件+失效条件（必须量化：关键价位+0.8%+2根K线）}
综合评分与分类：评分=1-10；分类=A/B/C/D/E
全周期时间与价格预期（短 / 中 / 长各一条）：
短期（最小周期 1-3 根 K 线）：时间跨度 =；目标价 =；触发 =；失效 =
中期（中周期 1-3 根 K 线）：时间跨度 =；目标价 =；触发 =；失效 =
长期（最大周期 1-3 根 K 线）：时间跨度 =；目标价 =；触发 =；失效 =
三段交易周期情景树（固定三段、各给 3 情景）：
未来 1-2 个交易日：情景 1/2/3（每条：触发 / 区间 / 切换临界）
未来 3-5 个交易日：情景 1/2/3（同上）
未来 1-2 周：情景 1/2/3（同上）
最终操作建议（不得新增点位）：买卖点=；止损=；止盈=；关键时间窗口=（引用T1/T2/T3）